namespace Cuni.Arithmetics.FixedPoint
{
    public interface IPrecision
    {
        //To satisfy this interface, just create an empty class with name format Q<Precision before dot>_<Precision after dot> where the precision sum is 32
    }
}
